"""Calix v3 — rule-table local Module Selector Agent.

The package layout mirrors v1; the behavioural difference is in
``agent.py`` which implements the same mode/clock/position rules using a
deterministic rule table (no network, no API calls).
"""
