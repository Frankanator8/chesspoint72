"""Forward pruning module — null-move, futility, razoring, LMR, zugzwang.

Algorithmic core only. The PruningConfig struct, default values, and any
top-level wiring live elsewhere (handled by a separate prompt / module).
"""
